# CLAUDE.md（業務ツール開発の最小ルール・雛形）

このファイルはプロジェクトのルートに置き、Claude Code に毎回読ませる前提のものです。中身は最小限の骨組みなので、自分のプロジェクトの目的・商品構成・数字は書き足してください。

## 目的
このリポジトリで、Claude Code を使って業務ツール（自動化スクリプト・スプレッドシート連携・簡易 Web アプリなど）を開発・運用する。ここに書くのは実行の土台（分担・柵・進め方）で、プロジェクト固有の目的や仕様は別の節に書き足す。

## 絶対ルール（詳細は .claude/rules/ に置く）
- 個人名・個人メール・会社名・顧客名を出力・コミット・設定に含めない。例外は法的に必要な場面（契約書・登記等）だけで、その入力は人が直接行い、リポジトリには書かない。
- 秘密情報（API キー・トークン・パスワード）は Secret Manager や環境変数の管理サービスにのみ置く。`.env` や `secrets/` はリポジトリに含めず、Claude にも読ませない。
- 利用者・顧客のデータは、それが置かれている環境の外に持ち出さない。外部 API（LLM を含む）に渡す前に匿名化する。
- 使うプラットフォームの規約（自動投稿・自動応答・外部ツール利用の可否）を先に確認し、禁止されている操作は自動化しない。

## 人に残す判断（既定は自分（Claude）が実行する）
既定は Claude が実行する。人に残すのは次の4つだけにする。
1. 本人確認・決済口座・契約（規約への同意を含む）
2. 規約で自動操作が禁止されている、またはアカウント停止のリスクがある操作
3. お金の決定（価格・値引き・返金）
4. 安全機構（hooks・permissions）が止めた操作

上の4つに当たらない送信・公開は、公開前検査（有料版の `compliance-checker` 相当、無ければ自分でチェックリストを用意する）を通してから Claude が実行し、結果を報告する。

## 分担（.claude/agents/）
- `reviewer` … Merge 前の最終レビュー。柵・匿名・テストの有無を見る（コードの好みは見ない）
- `researcher` … Web 調査・規約確認・ログの要点抽出（本体のコンテキストを使わずに要約だけ返す）
- `test-runner` … テスト実行、失敗だけを返す
- 必要に応じて増やす：公開前検査、キット・商品ごとのビルド担当など

## モデルの使い分け（例。プロジェクトの利用枠に合わせて決める）
- 設計・難所の相談：上位モデル（使用量の上限に注意）
- 実装・テスト：標準モデル
- 定型作業（翻訳・分類・要約）：軽量モデル、サブエージェントに任せる

## コンテキストの節約
- 検索・調査・ログ読み・テスト実行は本体で読まずサブエージェントに任せ、要約だけ受け取る。
- 無関係な作業に移るときはコミットしてから `/clear`。長い作業の前は `/compact`。
- ファイルの全文を `cat` しない。200行を超えるものは範囲指定するか、探索用のサブエージェントに読ませる。

## 進め方
- 大きい変更は plan mode で計画 → 承認 → 実装。並行作業は git worktree を使う。
- テストが通らないとコミットしない（`.claude/hooks/require-tests.sh` が編集後にテストを回す）。
- 破壊的操作（`rm -rf`、force push、`terraform apply` 等）は人が実行する（`.claude/hooks/block-destructive.sh` が止める）。
- 判断に迷ったら ADR（意思決定記録）を1枚書いてから進める。
- 人がやる作業に当たったら、それを書き留めるファイル（例：`docs/human-tasks.md`）に追記して止まる。勝手に代替しない。

## 応答の末尾（人のターン）
判断・作業を人に戻すときの書式は `.claude/rules/human-turn.md` を参照する。

# Compact instructions
圧縮するときは、未完了の人の判断待ち、承認待ちの決定事項、変更してコミットしたファイル一覧、実行中のバックグラウンド処理を残す。ツール出力の本文・検索結果・ファイル内容は捨ててよい。
